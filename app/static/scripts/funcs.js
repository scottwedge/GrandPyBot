const questionVal = document.querySelector('.inputSearch');
const apply = document.querySelector('.btn');

const getResponse = ()=>{
   const response = questionVal.value.trim();
   console.log(response);
};

// function to get the longetitude and laltitude
const getLatLong = async (query)=> {
 
}